# Marketing Shell — Public Pages

Shared by: Landing (`/`), Pricing (`/pricing`), About (`/about`), Blog (`/blog`).

## Navbar

Fixed top, height 52px, background `#FFFFFF`, border-bottom `1px solid rgba(55,53,47,0.09)`, z-index 50.

Content max-width 1100px, centered. Padding 0 24px.

Left: Logo icon (24px) + "Deskmate" wordmark (16px, weight 600, color `#37352F`).

Center: nav links — "Product", "Pricing", "About". Each: 15px, weight 500, color `#37352F`. Hover: bg `rgba(55,53,47,0.08)`, border-radius 6px, padding 6px 12px.

Right: Language toggle (14px, ghost) + "Sign in" text link (15px, weight 500) + "Get started" button (primary dark, 15px, height 36px, border-radius 8px).

### Mobile (< 1024px)
Right side becomes hamburger icon. Opens full-screen overlay: vertical menu items + CTA buttons at bottom. Close with X or ESC.

## Footer

Top border `1px solid rgba(55,53,47,0.09)`. Padding-top 64px, padding-bottom 32px. Max-width 1100px centered.

4-column grid (desktop), 2-column (tablet), 1-column (mobile). Gap 32px.

Column headers: 14px, weight 600, color `#37352F`.
Links: 14px, weight 400, color `rgba(55,53,47,0.6)`. Hover: color `#37352F`.

Columns:
1. Product: Features, How it works, Subjects, Demo
2. Resources: Blog, AP Courses, Templates
3. Company: About, Pricing, Privacy, Terms, Contact
4. Brand column: Logo + one-line description + social icons (Twitter, GitHub)

Bottom bar: copyright "© 2026 Deskmate" left, "Privacy · Terms" links right. 13px, color tertiary.
