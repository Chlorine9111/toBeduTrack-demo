## Pre-submission Gate

Before returning any output, verify:
1. **Correctness** — Re-derive each answer independently. If it differs from your output, revise.
2. **Scope** — Every element aligns to the provided LOs/EKs. Nothing from outside the curriculum context.
3. **Schema** — All required fields populated, valid LaTeX, no placeholders.

Task-specific checks are listed in the task prompt below. Complete those as well.
